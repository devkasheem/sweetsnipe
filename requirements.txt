# Requirements for Sweetsnipe with Security & Architecture Fixes

# Core dependencies (existing)
fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.36
alembic==1.13.3
python-dotenv==1.0.1
cryptography==43.0.1
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.12
aiofiles==24.1.0
pydantic==2.9.2
pydantic-settings==2.6.1
web3==7.12.0
eth-account>=0.8.0
aiohttp==3.11.7
rich==13.9.4
jinja2==3.1.4
websockets==13.1
psutil==6.1.1
slowapi==0.1.9
markupsafe==2.1.3
email-validator==2.1.0
psycopg2-binary==2.9.9

# Database support
# NOTE: For Python 3.13, PostgreSQL packages need compilation
# For development, SQLite works fine (no compilation needed)
# For production with PostgreSQL, either:
#   1. Downgrade to Python 3.11 or 3.12, OR
#   2. Install PostgreSQL dev libraries and build from source
# psycopg2-binary==2.9.9  # Uncomment when using Python 3.11/3.12
# asyncpg==0.29.0  # Uncomment when using Python 3.11/3.12

# Optional: Secrets management (install based on deployment)
# boto3==1.34.0  # AWS Secrets Manager
# hvac==2.1.0    # HashiCorp Vault

# Optional: Caching layer
# redis==5.0.1
# aioredis==2.0.1

# Optional: Structured logging
# structlog==24.1.0
# python-json-logger==2.0.7

# Testing (development only)
# pytest==8.0.0
# pytest-asyncio==0.23.0
# pytest-cov==4.1.0
# httpx==0.26.0  # For testing FastAPI
